import { ReactElement } from 'react';

//import Css
import className from './OutcomeStats.module.css';

export const OutcomeStats = (): ReactElement => {
  return (
    <div className={className.outcomeStats}>
      
    </div>
  );
};